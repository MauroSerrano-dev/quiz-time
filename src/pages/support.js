import styles from '../styles/support.module.css'

export default function Lobby(props) {
    const { session } = props

    return (
        <div>
            <main>
                Support
            </main>
        </div>
    )
}